# No secrets are required for the default run. Planetary Computer NAIP is
# open access and the TRPA REST services are public.
# Add keys here only if a later step needs them (for example a Nearmap API key).
NEARMAP_API_KEY=
